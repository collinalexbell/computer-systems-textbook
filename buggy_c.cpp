#include <stdio.h>

   float sum_elements(float a[], unsigned length){
      int i;
      int sum;
      for(i=0; i<=length-1; i++){
        printf("%d", i);
      }
      return 0.0;
    }
   int main(){
     float elements[0];
     sum_elements(elements ,0);
   }
  